# ============================================================
# Sigcore — Terraform Variables (EXAMPLE)
# ============================================================
# Copy this file to terraform.tfvars and fill in actual values.
# NEVER commit terraform.tfvars to version control!
#
# Note: No Twilio keys here — each tenant provides their own
#       credentials stored encrypted in the database.
# ============================================================

# --- AWS ---
aws_region  = "us-east-1"
environment = "prod"

# --- Database (RDS — created fresh on AWS) ---
db_password = "your-strong-db-password"
db_username = "sigcore"
db_name     = "sigcore"

# --- Encryption ---
# Railway > Sigcore (production) > Variables > ENCRYPTION_KEY
encryption_key = "your-64-char-hex-key"

# --- Service Auth ---
# Railway > Sigcore (production) > Variables > SIGCORE_SERVICE_KEY
sigcore_service_key = "your-sigcore-service-key"

# --- Loghub ---
loghub_url    = "https://geosloghub-production.up.railway.app"
loghub_source = "sigcore"
loghub_key    = "RAILWAY_INGEST_KEY_123"
